  <%= link_to question_path(question), method: :get do %>
  <div class="product" id="<%= 'question_' + question.id.to_s %>">
    <div class="product-upvote">
      <%= link_to like_question_path(question), method: :put do %>
      <i class="fa fa-arrow-circle-up"></i><% end %>
        <div class='product-count'><%= question.get_upvotes.size %></div>
    </div>
    <div class="product-body">
      <h3><%= link_to "#{question.title}", question_path(question), class:"link-question" %></h3>
      <p><%= question.description %></p>
    </div>
    <div class="product-controls" id ="action-button">
      <% if user_is_question_author == true %>
      <%= link_to "", question, method: :delete, class:"fa fa-trash" %>
      <%= link_to "", edit_question_path(question), class:"fa fa-pencil" %>
      <%= link_to "", archive_question_path(question), method: :put, class:"fa fa-check" %>
      <% end %>
    </div>
  </div>
  <div class="answer">
    <% if question.answer != nil  %>
      <%= link_to "voir la réponse", question_path(question), class:"link-question" %>
    <% end  %>
  </div>

<% end %>
